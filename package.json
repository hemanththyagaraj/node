{
  "name": "fullstackopen-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "rm -rf dist && cd frontend && npm run build && cp -r dist ../",
    "deploy": "npm run build && npm start"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}